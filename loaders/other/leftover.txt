
            # top_count_inds = np.argpartition(counts, -limit)[-limit:]
            # rare_counts = counts[~top_count_inds] # ~ everything not these indices
            #  rare_words = [token for token, count in token_count.items() if count > limit]